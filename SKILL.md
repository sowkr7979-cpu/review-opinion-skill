---
name: 검토의견서
description: "회계·세무 쟁점에 대한 회계 검토 의견서를 HTML → PDF → Word 로 만드는 절차. 존댓말 서술, 로마숫자 장, 장마다 결론 박스 + 도해, 기준서·질의회신 원문 발췌 각주, 근거 규정 전면 하이퍼링크, 쪽머리·쪽밑 PDF, Word 변환까지. 트리거: 검토의견서, 회계 검토 의견서, 의견서 v2/v3, 추가본, '이 양식으로'."
argument-hint: "[쟁점 제목] [의뢰회사] [v번호|추가본]"
metadata:
  version: "1.0.0"
---

# 회계 검토 의견서 스킬

의뢰회사에 제출하는 **회계 검토 의견서**를 한 번에 세 형식(HTML · PDF · Word)으로 만든다.
디자인·구성·문체·각주·링크 규칙이 모두 정해져 있으므로 **내용만 채우고 스크립트로 조립**한다.

## 0. 시작 전 확인

- 사실관계·계약 조항·질의 사항을 받았는가. 없으면 전제로 명시하고 "확인이 필요한 사항" 표에 넣는다.
- 근거는 기준서 원문·기준원 질의회신·법령 원문·공시 자료만 쓴다. 웹 검색 출처는 쓰지 않는다.
- 이전 판(v1/v2)이 있으면 그 HTML을 기준으로 **덧붙이고 고친다**(처음부터 다시 쓰지 않음). "추가본"은 새로 추가한 장만 따로 묶은 문서를 하나 더 만든다.

## 1. 문서 구조 (순서 고정)

1. **표지** — 로고(`{{LOGO}}`) · 제목 · "회계 검토 의견서" · 초록 줄 · "○○○ 주식회사 귀중" · 쟁점 한 줄 · 법인명 · 발행일 · 면책문
2. **검토 결과의 요약** — 초록 `.concl` 종합 결론(2~3문장) → `그림 1. 한 장으로 보는 결론`(4패널 SVG) → 파란 `.note` 1개
3. **Ⅰ. 검토의 배경과 범위** — 거래 구조 도해 → `표 1. 검토한 쟁점과 근거 규정·해당 장` → 전제 목록
4. **Ⅱ ~ Ⅹ 쟁점별 장** — 장마다 `결론 박스 1개 → 도해 1~2개 → 표 0~1개 → 짧은 문단·타일 → 각주 상자`
5. **마지막 장** — 대비표/실행 일정 도해 → `확인이 필요한 사항` 표 → `인용한 기준서 문단과 질의회신` 표 → 안내 문단(`p.sm.muted`) → 서명 블록
6. 요약 장에 "이번 개정판에서 보완한 사항" 같은 **개정 안내 박스는 넣지 않는다**.

## 2. 문체·표기 규칙 (references/style-rules.md 요약)

- **보고 5원칙을 적용한다.** ① 모든 내용을 똑같이 중요하게 다루지 않는다 ② 가장 중요한 하나를 먼저 고른다(결론 박스가 그 자리다) ③ 좋은 말 대신 숫자와 사실을 쓰고 측정값과 가정을 구분한다 ④ 문제·한계를 숨기지 않는다("확인이 필요한 사항" 표가 그 자리다) ⑤ 마지막에 "그래서 무엇을 해야 하는가" 판단을 넣는다(실행 권고안 장). 금지어(`대부분`·`일부`·`양호`·`적절히`·`~로 보입니다` 등)도 동일하게 적용한다.
- **존댓말 서술체** "~합니다 / ~하셔야 합니다". 결론 박스 첫 문장에 결론, 두 번째 문장에 이유.
- **의문형 제목·표현 금지**: "~인가 / ~는가 / ~할 것인가" → 명사형("비지배지분의 인식 여부", "○○ 여부"). 플로차트 분기도 "○○ 여부".
- 영어 약어·개발 용어 금지(고유명사 예외). 숫자는 표·타일·도해에, 문장 안에는 최소.
- **텍스트는 줄이고 도해로**: 문단은 1~2문장(`p.one`), 세 가지 이상 나열은 `.tiles3`, 절차·판단은 흐름도, 시점은 타임라인, 대비는 두 패널, 수치 비교는 가로 막대.
- 분개는 `.je` 카드(차변 | 대변 · 아래 한 줄 이유). 둘씩 `.jgrid`.

## 3. 각주 규칙

- 본문 표시: `<sup class="fn" data-fn="KEY"></sup>`, 장 끝: `<div class="fnbox" data-fns="KEY1,KEY2"></div>` → `scripts/finalize.py`가 번호 매기고 본문을 채운다.
- 각주 사전(`footnotes.json`) 항목: `url`, `title`("기준서 제○○○○호 문단 N"), `excerpt`(원문 발췌). 생략은 **"― 중략 ―"**, 인용문에 **굵게·밑줄 등 장식 없음**. 형식: `제○○호 문단 N — “…원문… ― 중략 ― …원문…”`.
- 원문은 반드시 기준서 원문에서 확보한 문단 텍스트를 쓴다(기억으로 쓰지 않음). 신속처리질의는 "기준원 연구원의 개인적 견해로 공식 의견과 다를 수 있음"을 덧붙인다.
- **SVG `<text>` 안에는 `<sup>`·`<a>`를 넣지 않는다**(SVG가 깨져 뒤 내용이 사라짐). 각주·링크는 `figcaption`에 둔다.

## 4. 하이퍼링크 규칙

- 근거 규정은 **본문 어디서든 모두 링크**. `finalize.py`의 `linkify`가 "제○○○○호 (문단 N)"과 문맥상 기준서가 분명한 "문단 N"을 자동 링크한다. 문맥이 바뀌는 자리(직전에 다른 기준서가 언급된 뒤)에는 "제○○○○호 문단 N"처럼 **기준서를 명시**해 오인을 막는다. `check.py`가 오인 링크를 잡는다.
- 주소: 기준서 단위 `https://db.kasb.or.kr/s/{번호}`, 문단 딥링크는 `kasb-links.json`의 `sections`에 등록된 것만(자동 조회 금지, 브라우저에서 직접 복사한 주소만 등록). 질의회신·해석위원회 자료는 `www.kasb.or.kr/front/board/View{ctgCd}.do?...` 형식. **kifrs.com 링크 금지**(로그인 필요).
- **앵커 없는 문단 링크 금지.** `https://db.kasb.or.kr/s/{번호}`는 그 기준서의 **문단 1**로 열린다. 따라서 링크 문구가 "문단 N"을 가리키는데 주소에 `/{섹션ID}`가 없으면 **독자는 인용 문단을 볼 수 없다.**
  - 앵커가 `sections`에 없으면 **링크 문구에서 문단 번호를 빼고 기준서명만 링크**한다. 문단 번호는 링크 밖 본문에 그대로 둔다. 예: `<a>제○○○○호</a> 문단 N` (○) / `<a>제○○○○호 문단 N</a>` (✗).
  - 문단까지 링크하려면 브라우저에서 해당 절을 열어 주소를 복사해 `sections`에 등록한다.
  - `check.py`가 이 위반을 "문단 앵커 없는 링크 N종"으로 보고한다. **0건이어야 배포한다.**
  - 한 문구에 문단이 여럿일 때(`문단 A·B`, `문단 A → B → C`): 모두 같은 절이면 그 앵커 하나로, **각기 다른 절이면 문단 번호마다 자기 절로 따로 건다**(`제○○○○호` 는 기준서 링크, 숫자는 각각 딥링크). 일부만 앵커가 있으면 기준서만 링크하고 문단은 평문으로 둔다.
- **앵커를 새로 받으면 `relink.py` 로 전 산출물을 한 번에 다시 건다.** `python scripts/relink.py kasb-links.json a.html b.html ...` — 원본(백업) HTML 을 복원한 뒤 실행하면 재고에 맞춰 딥링크·기준서링크·평문을 다시 배분한다. 링크를 지웠다 다시 거는 작업을 손으로 하지 않는다.
- **db.kasb 링크는 HTTP로 검증되지 않는다.** SPA라서 존재하지 않는 주소도 정상 응답(200, 동일 69,912바이트)을 준다. 자동 검증 수단이 없으므로 새 앵커는 **시크릿 창에서 눈으로 한 번 확인**하고 `kasb-links.json`에 `확인일`과 함께 등록한다.
- 각주 발췌문 안과 SVG 안은 링크하지 않는다.

## 5. 도해 규칙 (references/diagrams.md)

- 팔레트: navy `#00153D` · green `#3F9C35` · blue `#009CDE` · red `#C0392B` · amber `#E8A33D` · 회색 `#515356/#888B8D/#CDCECF/#F0F0F0`. 차트 계열색은 `#1F77C0`(파랑) + `#C0392B`(빨강)이 색 대비 검증 통과, 초록+빨강은 경계(ΔE 6.0)라 라벨을 반드시 병기.
- `viewBox="0 0 720 H"`, 글자 7.6~11pt, `role="img" aria-label` 필수, 모든 도형에 명시적 fill.
- 필수 패턴: 4패널 결론 요약 · 거래 구조 · 판단 흐름도(알약 질문 + 예/아니오 + "이 사안" 표식) · 단계 체인(`.steps`) · 시점 타임라인 · 두 패널 대비 · 수직선 위치 도해 · 가로 막대 비교 · 산식/구간 선 그래프 · 실행 일정.
- 계열색을 바꿀 때는 색 대비(적록색약 포함)를 검증한 뒤 쓴다.

## 6. 조립·빌드 절차

```text
1) templates/report_template.html 의 {{...}}(LOGO·FIRM_KR·FIRM_EN·YEAR 포함) 를 채우고 장 HTML 을 작성   (그림·표 번호는 "그림 §." / "표 §." 로 둠)
2) python scripts/finalize.py in.html out.html --footnotes footnotes.json --links kasb-links.json
      → 그림·표 번호, 각주 번호·본문, 근거 링크 자동 처리
3) python scripts/check.py out.html
      → 의문형 표현 · 오인 링크 · SVG 오염 · 미링크 근거 · 남은 {{ }} 를 보고
4) python scripts/build.py out.html --pdf 결과.pdf --docx 결과.docx --footer "의뢰회사 | 제목 | 회계 검토 의견서"
      → Chrome 인쇄 + 쪽머리(로고·남색선)·쪽밑(초록선·문서명·쪽번호) → PDF, SVG 를 PNG 로 바꿔 Word COM 으로 .docx
5) PDF 몇 쪽을 PNG 로 렌더해 한 번만 눈으로 확인(도해 깨짐·표 넘침) → 수정은 1회
6) out.html 을 게시하고 파일 목록을 보고
```

- 이전 판을 고치는 경우: 예제처럼 `must_replace(html, old, new)`로 **정확한 문자열 치환** 후 새 장을 삽입하고, 구 장 번호(Ⅷ→Ⅺ 등)를 옮긴다. 본문의 장 참조는 `{{CH_xxx}}` 자리표시자로 두고 문서별로 치환하면 v3·추가본에 같은 조각을 재사용할 수 있다.
- 산출물 파일명: `주제_검토의견서_v3_YYYY-MM-DD.html`, `주제_검토의견서v3.pdf`, `.docx`, 추가본은 `_추가본`.

## 7. 끝내기 전 점검

- [ ] 장마다 결론 박스 1개, 도해 1개 이상, 각주 상자 1개
- [ ] 의문형 0건, 오인 링크 0건, SVG 오염 0건, **문단 앵커 없는 링크 0종** (`check.py`)
- [ ] 금액 예시는 "가정치"임을 표지 면책문·표 캡션에 명시
- [ ] 확인이 필요한 사항 표에 미제공 정보 수록
- [ ] PDF 쪽수·Word 쪽수 보고

## 파일

- `templates/report_template.html` — 전체 CSS + 표지 + 요약 뼈대 + 서명
- `templates/components.html` — 결론·주의·참고 박스, 표, 그림, 분개 카드, 단계 체인, 3칸 타일, 각주 표시 스니펫
- `references/style-rules.md` — 문체·표기·분량 규칙 전문
- `references/diagrams.md` — 도해 패턴 카탈로그와 좌표 관례
- `references/footnotes.example.json` — 각주 사전 예시(형식)
- `references/kasb-links.example.json` — 링크 규칙과 확인된 딥링크
- `scripts/finalize.py`, `scripts/check.py`, `scripts/build.py`
- `scripts/relink.py` — `kasb-links.json` 앵커 재고에 맞춰 완성본 HTML 의 db.kasb 링크를 다시 배분(딥링크 / 기준서만 / 평문). 앵커를 새로 확보할 때마다 원본 복원 후 실행한다.
- `assets/logo.png` — 표지·쪽머리 로고. 직접 넣는다(저장소에 포함하지 않음).
